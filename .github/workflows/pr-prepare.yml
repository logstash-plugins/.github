name: Prepare PR

on:
  workflow_call:
    inputs:
      command:
        type: string
        description: 'Command: release:patch, release:minor, release:major, or changelog:add'
      pr_number:
        type: string
        description: 'PR number to link in the changelog entry'
      changelog_entry:
        type: string
        description: 'Custom changelog entry (defaults to PR title if omitted)'
    secrets:
      CLIENT_ID:
        required: false
        description: 'GitHub App Client ID for automated commits'
      APP_PRIVATE_KEY:
        required: false
        description: 'GitHub App private key PEM for automated commits'

jobs:
  notify-instructions:
    name: Post instructions
    runs-on: ubuntu-latest
    if: github.event.action == 'opened'
    permissions:
      pull-requests: write
    steps:
      - name: Post comment
        uses: actions/github-script@v9
        with:
          script: |
            const owner = context.payload.repository.owner.login;
            const repo = context.payload.repository.name;
            const prNumber = context.payload.pull_request.number;

            const files = await github.paginate(github.rest.pulls.listFiles, {
              owner, repo, pull_number: prNumber, per_page: 100
            });
            const filenames = files.map(f => f.filename);

            const hasChangelog = filenames.includes('CHANGELOG.md');
            const hasVersion = filenames.includes('version') || filenames.includes('VERSION');

            let body;
            if (hasChangelog) {
              if (hasVersion) {
                core.info('PR already includes changelog and version changes, skipping comment');
                return;
              }
              body = `### Changelog and Version Management

              This PR updates the changelog but does not update the plugin version. Please check if the current plugin version is already published on [RubyGems](https://rubygems.org/). If it is, a version bump is needed.`.replace(/^              /gm, '');
            } else {
              body = `### Changelog and Version Management

              No changelog or version changes detected. You can either update them manually or use a comment command to update them automatically on merge:

              \`/pr_prepare release:<patch|minor|major>\` — bump version + update changelog
              \`/pr_prepare changelog:add\` — changelog entry only (no version bump)

              \`\`\`
              /pr_prepare release:patch
              - Add the changelog entry here
                - (Additional details can be provided in multiple lines, respecting markdown formatting)
              \`\`\`

              Omit the entry line to use the PR title. If multiple commands are posted, the last one wins.`.replace(/^              /gm, '');
            }

            await github.rest.issues.createComment({ owner, repo, issue_number: prNumber, body });

  prepare-pr:
    name: Update changelog and version
    runs-on: ubuntu-latest
    if: >-
      (github.event.action == 'closed' && github.event.pull_request.merged == true) || inputs.pr_number
    permissions:
      contents: write
    env:
      USE_APP_TOKEN: ${{ secrets.CLIENT_ID != '' }}
    # Flow: generate app token → resolve inputs (from comment or dispatch) → fetch files → compute updates → commit
    steps:
      - name: Generate GitHub App token
        id: app-token
        if: env.USE_APP_TOKEN == 'true'
        uses: actions/create-github-app-token@v3.2.0
        with:
          client-id: ${{ secrets.CLIENT_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - name: Resolve inputs
        id: resolve
        uses: actions/github-script@v9
        env:
          INPUT_COMMAND: ${{ inputs.command }}
          INPUT_PR_NUMBER: ${{ inputs.pr_number }}
          INPUT_CHANGELOG_ENTRY: ${{ inputs.changelog_entry }}
        with:
          github-token: ${{ steps.app-token.outputs.token || github.token }}
          script: |
            const owner = context.payload.repository.owner.login;
            const repo = context.payload.repository.name;
            // strip all whitespace — the format is `release:<something>` with no spaces anywhere
            const inputCommand = (process.env.INPUT_COMMAND || '').replace(/\s+/g, '');
            const inputPrNumber = process.env.INPUT_PR_NUMBER;
            const inputEntry = process.env.INPUT_CHANGELOG_ENTRY;

            const isManualDispatch = inputPrNumber !== '';

            let prNumber, prTitle, baseRef, command, rawEntryText;

            if (isManualDispatch) {
              const { data: pr } = await github.rest.pulls.get({
                owner, repo, pull_number: parseInt(inputPrNumber)
              });

              if (!pr.merged) {
                throw new Error(`PR #${pr.number} is not merged`);
              }

              prNumber = parseInt(inputPrNumber);
              prTitle = pr.title;
              baseRef = pr.base.ref;
              command = inputCommand;
              const trimmed = inputEntry ? inputEntry.trim() : inputEntry;
              rawEntryText = trimmed && !trimmed.startsWith('- ')
                ? `- ${trimmed}`
                : trimmed;

              const validCommands = ['release:major', 'release:minor', 'release:patch', 'changelog:add'];
              if (!validCommands.includes(command)) {
                throw new Error(`Invalid command: ${command}`);
              }
            } else {
              prNumber = context.payload.pull_request.number;
              prTitle = context.payload.pull_request.title;
              baseRef = context.payload.pull_request.base.ref;

              // Skip if PR already modified changelog
              const files = await github.paginate(github.rest.pulls.listFiles, {
                owner, repo, pull_number: prNumber, per_page: 100
              });
              if (files.some(f => f.filename === 'CHANGELOG.md')) {
                core.info('PR already includes changelog changes, skipping automation');
                core.setOutput('found', 'false');
                return;
              }

              const comments = await github.paginate(github.rest.issues.listComments, {
                owner, repo, issue_number: prNumber, per_page: 100
              });

              // Only consider comments from users with write or admin access or the PR author.
              const prAuthor = context.payload.pull_request.user.login;
              const candidateComments = comments.filter(c => c.body.trim().startsWith('/pr_prepare'));
              const prPrepareComments = [];
              for (const c of candidateComments) {
                if (c.user.login === prAuthor) {
                  prPrepareComments.push(c);
                  continue;
                }
                const { data: perm } = await github.rest.repos.getCollaboratorPermissionLevel({
                  owner, repo, username: c.user.login
                });
                if (['admin', 'write'].includes(perm.permission)) {
                  prPrepareComments.push(c);
                }
              }
              if (prPrepareComments.length === 0) {
                core.info('No /pr_prepare comment found, skipping');
                core.setOutput('found', 'false');
                return;
              }

              const commentBody = prPrepareComments[prPrepareComments.length - 1].body;
              const firstLine = commentBody.split('\n')[0].trim();
              const commandMatch = firstLine.match(/^\/pr_prepare\s+(release:major|release:minor|release:patch|changelog:add)$/);
              if (!commandMatch) {
                throw new Error(`Invalid /pr_prepare command: ${firstLine}`);
              }
              command = commandMatch[1];
              rawEntryText = commentBody.split('\n').slice(1).join('\n');
            }

            if (command === 'release:major' && baseRef !== 'main') {
              throw new Error('release:major is only supported on PRs targeting main');
            }

            // Build changelog entry
            const prUrl = `https://github.com/${owner}/${repo}/pull/${prNumber}`;
            const entryLines = rawEntryText.split('\n').filter(l => l.trim().length > 0);

            let entry;
            if (entryLines.length > 0) {
              const formatted = entryLines.map(line => {
                if (line.match(/^- /)) {
                  const text = line.replace(/^-\s*/, '').trim();
                  return `  - ${text} [#${prNumber}](${prUrl})`;
                }
                return `  ${line}`;
              });
              entry = formatted.join('\n');
            } else {
              const unsafeChars = prTitle.match(/[^a-zA-Z0-9 _.,:;+=#@()\-`'!?&~]/g);
              if (unsafeChars) {
                throw new Error(`PR title contains unsupported characters: ${[...new Set(unsafeChars)].join(' ')}`);
              }
              entry = `  - ${prTitle} [#${prNumber}](${prUrl})`;
            }

            core.setOutput('found', 'true');
            core.setOutput('command', command);
            core.setOutput('entry', entry);
            core.setOutput('base-ref', baseRef);

      - name: Fetch version and changelog files
        if: steps.resolve.outputs.found == 'true'
        id: fetch-files
        uses: actions/github-script@v9
        env:
          BASE_REF: ${{ steps.resolve.outputs.base-ref }}
          COMMAND: ${{ steps.resolve.outputs.command }}
        with:
          github-token: ${{ steps.app-token.outputs.token || github.token }}
          script: |
            const fs = require('fs');
            const owner = context.payload.repository.owner.login;
            const repo = context.payload.repository.name;
            const ref = process.env.BASE_REF;
            const command = process.env.COMMAND;

            if (command !== 'changelog:add') {
              for (const path of ['version', 'VERSION']) {
                try {
                  const { data } = await github.rest.repos.getContent({ owner, repo, path, ref });
                  fs.writeFileSync('version-content', Buffer.from(data.content, 'base64').toString('utf8'));
                  core.setOutput('version-path', path);
                  break;
                } catch (e) {
                  if (path === 'VERSION') throw new Error('No version file found');
                }
              }
            }

            const { data: changelogFile } = await github.rest.repos.getContent({
              owner, repo, path: 'CHANGELOG.md', ref
            });
            fs.writeFileSync('changelog-content', Buffer.from(changelogFile.content, 'base64').toString('utf8'));

      - name: Compute new version and changelog
        if: steps.resolve.outputs.found == 'true'
        id: compute
        uses: actions/github-script@v9
        env:
          COMMAND: ${{ steps.resolve.outputs.command }}
          ENTRY: ${{ steps.resolve.outputs.entry }}
        with:
          script: |
            const fs = require('fs');
            const command = process.env.COMMAND;
            const entry = process.env.ENTRY;

            const changelog = fs.readFileSync('changelog-content', 'utf8');

            // Guard against duplicate entries (re-runs or dispatch after automated run)
            const prLink = entry.match(/(\/pull\/\d+\))/);
            if (prLink && changelog.includes(prLink[1])) {
              throw new Error('Changelog already contains an entry for this PR');
            }

            let newChangelog;

            if (command === 'changelog:add') {
              const lines = changelog.split('\n');
              if (!lines[0].startsWith('## ')) {
                throw new Error('Unexpected CHANGELOG.md format: expected first line to be a version header (e.g. `## x.y.z` or `## Unreleased`)');  
              }
              lines.splice(1, 0, entry);
              newChangelog = lines.join('\n');
              fs.writeFileSync('new-changelog', newChangelog);
              fs.writeFileSync('commit-message', 'chore: Add changelog entry');
            } else {
              const versionContent = fs.readFileSync('version-content', 'utf8');
              const match = versionContent.match(/(\d+)\.(\d+)\.(\d+)/);
              if (!match) throw new Error('Could not parse version number');
              let [, major, minor, patch] = match.map(Number);

              let newVersion;
              switch (command) {
                case 'release:major': newVersion = `${major + 1}.0.0`; break;
                case 'release:minor': newVersion = `${major}.${minor + 1}.0`; break;
                case 'release:patch': newVersion = `${major}.${minor}.${patch + 1}`; break;
              }

              newChangelog = `## ${newVersion}\n${entry}\n\n${changelog}`;
              fs.writeFileSync('new-changelog', newChangelog);
              fs.writeFileSync('new-version-content', versionContent.replace(/\d+\.\d+\.\d+/, newVersion));
              fs.writeFileSync('commit-message', `chore: Update changelog and bump version to ${newVersion}`);
            }

      - name: Create file blobs
        if: steps.resolve.outputs.found == 'true'
        id: create-blobs
        uses: actions/github-script@v9
        env:
          VERSION_PATH: ${{ steps.fetch-files.outputs.version-path }}
        with:
          github-token: ${{ steps.app-token.outputs.token || github.token }}
          script: |
            const fs = require('fs');
            const owner = context.payload.repository.owner.login;
            const repo = context.payload.repository.name;

            const newChangelog = fs.readFileSync('new-changelog', 'utf8');
            const { data: changelogBlob } = await github.rest.git.createBlob({
              owner, repo, content: newChangelog, encoding: 'utf-8'
            });

            const tree = [{
              path: 'CHANGELOG.md', mode: '100644', type: 'blob', sha: changelogBlob.sha
            }];

            if (fs.existsSync('new-version-content')) {
              const newVersionContent = fs.readFileSync('new-version-content', 'utf8');
              const { data: versionBlob } = await github.rest.git.createBlob({
                owner, repo, content: newVersionContent, encoding: 'utf-8'
              });
              tree.push({
                path: process.env.VERSION_PATH, mode: '100644', type: 'blob', sha: versionBlob.sha
              });
            }

            core.setOutput('tree', JSON.stringify(tree));

      - name: Commit and push
        if: steps.resolve.outputs.found == 'true'
        uses: actions/github-script@v9
        env:
          BASE_REF: ${{ steps.resolve.outputs.base-ref }}
          TREE_JSON: ${{ steps.create-blobs.outputs.tree }}
        with:
          github-token: ${{ steps.app-token.outputs.token || github.token }}
          script: |
            const fs = require('fs');
            const owner = context.payload.repository.owner.login;
            const repo = context.payload.repository.name;
            const baseRef = process.env.BASE_REF;
            const message = fs.readFileSync('commit-message', 'utf8').trim();
            const tree = JSON.parse(process.env.TREE_JSON);

            const { data: ref } = await github.rest.git.getRef({
              owner, repo, ref: `heads/${baseRef}`
            });
            const baseSha = ref.object.sha;

            const { data: baseCommit } = await github.rest.git.getCommit({
              owner, repo, commit_sha: baseSha
            });

            // Include the original commit message in the new commit to provide more context in the history.
            const prevMessage = baseCommit.message.split('\n')[0];
            const fullMessage = `${message} for '${prevMessage}'`;

            const { data: newTree } = await github.rest.git.createTree({
              owner, repo, base_tree: baseCommit.tree.sha, tree
            });

            const { data: newCommit } = await github.rest.git.createCommit({
              owner, repo, message: fullMessage,
              tree: newTree.sha,
              parents: [baseSha],
              author: {
                name: 'github-actions[bot]',
                email: '41898282+github-actions[bot]@users.noreply.github.com'
              },
              committer: {
                name: 'github-actions[bot]',
                email: '41898282+github-actions[bot]@users.noreply.github.com'
              }
            });

            await github.rest.git.updateRef({
              owner, repo, ref: `heads/${baseRef}`, sha: newCommit.sha
            });

            core.info(`${fullMessage} (${newCommit.sha.substring(0, 7)})`);
